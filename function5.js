function fatorial(n) {
  let fatorial = n;
  let result = fatorial;

  for (let i = 1; i < fatorial; i++) {
    result *= i;
  }
  return console.log(result);
}
fatorial(0);
fatorial(1);
fatorial(5);
fatorial(10);
